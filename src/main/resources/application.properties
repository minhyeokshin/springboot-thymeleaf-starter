# Application configuration for Spring Boot + Thymeleaf starter
# (????? + ???? ???? ?????? ??)

# Application name
# (?????? ??)
spring.application.name=springboot-thymeleaf-starter

# Import secret properties file (optional)
# (??? ?? ?? ???? - ???)
spring.config.import=optional:application-secret.properties

# Database configuration
# (?????? ??)
spring.datasource.url=${DB_URL}
spring.datasource.driver-class-name=${DB_DRIVER}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA DDL auto mode (create, update, validate, none)
# (JPA DDL ?? ?? - create, update, validate, none ? ??)
spring.jpa.hibernate.ddl-auto=create

# Thymeleaf template settings
# (???? ??? ??)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Disable cache in development by default
# (?? ? ??? ?? ???? ???)
spring.thymeleaf.cache=${THYMELEAF_CACHE:false}